<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: #f0f0f0; /* Fond gris clair */
            font-family: 'Arial', sans-serif;
        }
        .card {
            border-radius: 15px;
            background-color: #e6e6e6; /* Fond gris doux */
            border: none;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .card-title {
            color: #161414; /* Gris foncé pour le titre */
            font-weight: bold;
        }
        .form-control {
            border-radius: 10px;
            background-color: #f8f9fa; /* Gris très clair */
            border: 1px solid #ccc;
            box-shadow: none;
            transition: all 0.3s ease-in-out;
        }
        .form-control:focus {
            border-color: #888; /* Gris foncé */
            box-shadow: 0 0 5px rgba(136, 136, 136, 0.5);
        }
        .btn-primary {
            background-color: #555; /* Bouton gris foncé */
            border: none;
            border-radius: 10px;
            transition: all 0.3s ease-in-out;
        }
        .btn-primary:hover {
            background-color: #777; /* Gris légèrement plus clair */
        }
        .text-link {
            color: #555; /* Gris pour les liens */
            text-decoration: none;
        }
        .text-link:hover {
            color: #777; /* Changement de teinte au survol */
        }
        h1 {
            color: #000; /* Couleur noire */
            font-weight: bold;
            text-align: center;
            margin-bottom: 5px;
            text-transform: uppercase; /* Mettre le texte en majuscules */
            letter-spacing: 2px; /* Espacement des lettres */
            padding: 20px 0; /* Padding pour espacer le titre */
            background-color: #e6e6e6; /* Fond gris clair */
            border-radius: 10px; /* Coins arrondis */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2); /* Ombre douce */
        }
    </style>
</head>

<body class="bg-light">
   
    <div class="container">
        
        <div class="row justify-content-center align-items-center vh-100">
            <h1>Welcome to the Login Page</h1>
            <div class="col-md-6 col-lg-4">
                <div class="card shadow-sm">
                   
                    <div class="card-body">
                        
                        <h3 class="card-title text-center mb-4">Login</h3>
                        <form action="/login" method="POST">
                            <!-- Email Input -->
                            <div class="mb-3">
                                <label for="email" class="form-label">Email address</label>
                                <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email" required>
                            </div>
                            <!-- Password Input -->
                            <div class="mb-3">
                                <label for="password" class="form-label">Password</label>
                                <input type="password" class="form-control" id="password" name="password" placeholder="Enter your password" required>
                            </div>
                
                            <!-- Submit Button -->
                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary">Login</button>
                            </div>
                        </form>
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Bootstrap JS (Optional for interactivity) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
